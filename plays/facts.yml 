---
- hosts: awx_group
  become: yes
  tasks: 
    - name: update
      yum:
        name: "*"
        state: latest

  #  - ansible.builtin.file:
  #      path: /etc/ansible/facts.d
   #     state: directory
    #    recurse: yes

   # - ansible.builtin.copy:
     #   src: custom.fact
      #  dest: /etc/ansible/facts.d
